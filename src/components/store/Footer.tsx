import { Link } from "@tanstack/react-router";
import { site } from "@/data/site";
export function Footer() { return <footer id="contact" className="mt-16 border-t border-border bg-secondary/40"><div className="mx-auto grid max-w-[1400px] gap-10 px-4 py-14 md:grid-cols-2 md:px-8"><div><p className="font-display text-2xl uppercase tracking-[0.12em]">{site.name}</p><p className="mt-3 max-w-md text-sm leading-relaxed text-muted-foreground">{site.tagline}. Browse the collection, then message us to check availability and place your order.</p><Link to="/collections" className="mt-5 inline-block text-xs uppercase tracking-[0.16em] text-primary">Browse sarees</Link></div><div><p className="eyebrow">Contact & orders</p><p className="mt-4 text-sm text-muted-foreground">{site.contact.phone}</p><p className="mt-1 text-sm text-muted-foreground">{site.contact.address}</p><div className="mt-5 flex flex-wrap gap-3"><a href={site.whatsAppUrl("Hello, I would like to know more about your sarees.")} className="inline-flex min-h-12 items-center bg-primary px-6 text-xs uppercase tracking-[0.16em] text-primary-foreground">Chat on WhatsApp</a><a href={site.callUrl} className="inline-flex min-h-12 items-center border border-primary px-6 text-xs uppercase tracking-[0.16em] text-primary">Call Now</a></div></div></div></footer>; }
